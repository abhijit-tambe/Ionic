import { AddworkdayPipe } from './addworkday.pipe';

describe('AddworkdayPipe', () => {
  it('create an instance', () => {
    const pipe = new AddworkdayPipe();
    expect(pipe).toBeTruthy();
  });
});
